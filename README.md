# PokéDex React (Vite + Bootstrap + Axios) 🎯

Un sistema que consume **PokeAPI** y lo reenderiza para hacer uso de esa data.

## Tecnologías
- React + Vite
- React Router DOM
- Axios
- Bootstrap 5
- API: [PokeAPI](https://pokeapi.co/) — `GET /pokemon/ditto`

## Instalación
```bash
npm create vite@latest pokeapp -- --template react
cd pokeapp
npm i axios react-router-dom bootstrap
